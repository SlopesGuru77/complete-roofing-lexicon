# HANDOFF — 2026-05-22

**Project:** Roofing Lexicon: Field Campaign — sibling repo `roofing-lexicon-field-campaign`
**Working dir for next session:** `C:\Users\roofk\Dev\--FINAL ORGANIZED\roofing-lexicon-field-campaign`
**Predecessor handoff:** `HANDOFF-2026-05-21.md` (in this OLD Lexicon repo)
**Session focus:** Explore new illustration styles before committing to a final art direction across all 33 boss cards. The woodcut set (style A) was committed yesterday in `ba96523`, but the user wants to compare alternatives.

---

## 1. Current phase + wave status

- **Phase:** Phase 16b — Imagery / art-direction selection (carry-over from 2026-05-21)
- **Wave:** Style-exploration round 3 (round 1 = A–D on Hail Forensics; round 2 = E–H modern alternatives; round 3 = I–L new directions, this session)
- **Context at handoff:** ~30%

## 2. Completed work

- Generated 4 new completely-different style options on the Hail Forensics test subject:
  - **I — Cyanotype blueprint** (white drafting lines on Prussian blue, technical schematic)
  - **J — Comic book halftone** (Silver Age Kirby, Ben-Day dots, CMYK newsprint)
  - **K — Paper-craft diorama** (layered cut paper, real cast shadows)
  - **L — Mid-century modern poster** (Saul Bass, flat geometric, 4-color)
- User reviewed and **selected style I (cyanotype)** for further validation.
- Wrote 4 follow-up prompts to stress-test cyanotype across content types: `shingles-04` (ventilation diagram), `shingles-10` (warranty scrolls), `code-03` (adjuster portraits), `low-slope-05` (cool-roof atmospheric). **None generated** — see §4.

### Files changed

- 8 new files in `roofing-lexicon-field-campaign/_style-options/`:
  - `I.json`, `J.json`, `K.json`, `L.json` (prompt JSONs, round 3)
  - `I-cyanotype.png`, `J-comic-halftone.png`, `K-paper-craft.png`, `L-midcentury.png` (round 3 outputs, ~7.2 MB total)
- 4 new prompt JSONs for round 4 (cyanotype validation):
  - `I-batch-shingles-04.json`, `I-batch-shingles-10.json`, `I-batch-code-03.json`, `I-batch-low-slope-05.json`
- 4 corresponding `.log` files all showing failures
- All uncommitted; `_style-options/` is in `.gitignore` per `ba96523` so it stays local

### Commits this session

None. Round-3 PNGs are art-direction reference, not project assets.

### Recent commits on `main` (for context)

```
ba96523 feat(art): 33 woodcut boss illustrations + gitignore _style-options
6d533b8 chore: trigger rebuild for VITE_SUPABASE_ANON_KEY
846e10a feat(content): code bosses 08-11 + Mastery — zone complete
```

## 3. Remaining tasks

1. **Cyanotype validation batch (4 images)** — prompts written but not generated. Files ready at:
   - `_style-options/I-batch-shingles-04.json`
   - `_style-options/I-batch-shingles-10.json`
   - `_style-options/I-batch-code-03.json`
   - `_style-options/I-batch-low-slope-05.json`
2. **Final style decision** — confirm cyanotype (I) holds across diagram/document/portrait/atmospheric subjects, then either:
   - Lock I and re-run `_style-options/generate-all.sh` with the cyanotype style prefix to regenerate all 33 boss illustrations (currently committed as woodcut), OR
   - Keep the committed woodcut set and revert this exploration.
3. **Open carry-overs from 2026-05-21 handoff** (still unaddressed): PWA icons (192/512/maskable), `og-cover.png` (1200×630), `favicon.svg`, DNS A record for `play.justennewton.media` → `91.99.230.200`, Phase 17 cutover redirect on OLD Zeabur project.

## 4. Known blockers

- **inference.sh out of credits — HARD BLOCKER.** All 4 round-4 generations failed with `Failed to submit task: insufficient balance. add credits at: https://app.inference.sh/settings/billing`. Yesterday's full 33-card generation drained the account.
- TLS handshake timeouts also observed on 2 of the 4 calls (network-level flakiness from yesterday persists).
- CLI auto-update v1.8.3 → v1.13.9 was triggered mid-batch on one job; non-fatal but slows runs.
- `_style-options/` is ~7 MB and not committed (gitignored); round-3 PNGs will be lost if the working tree is wiped without a manual backup.

## 5. Exact resume command

```powershell
cd "C:\Users\roofk\Dev\--FINAL ORGANIZED\roofing-lexicon-field-campaign"
```

**Then, after topping up inference.sh credits:**

> Read `HANDOFF-2026-05-22.md` §3.1. Run the 4 prepped cyanotype validation prompts in parallel:
> ```
> for f in _style-options/I-batch-*.json; do
>   id=$(basename "$f" .json)
>   infsh app run google/gemini-3-1-flash-image-preview -i "$f" -o "_style-options/${id}.out.json" > "_style-options/${id}.log" 2>&1 &
> done; wait
> ```
> Then download each PNG via the URL in the `.out.json` files. If cyanotype holds across all 4 subjects, lock style I and regenerate all 33 boss illustrations.

---

## Update 21:55 CDT — Lexicon MVP launched + Supabase sync wired

> **Different project.** Everything above is the sibling `roofing-lexicon-field-campaign` repo. This update is about THIS repo (`The Commercial Roofing Lexicon`, the v3 single-file app deployed at `complete-roofing-lexicon.zeabur.app`).

### 1. Current phase + wave status

- **Phase:** v3.1 MVP launch (no GSD `.planning/` in this repo; informal phases).
- **Wave:** post-deploy operator handshake — Supabase sync test fix shipped, awaiting first real cert-sync round-trip from owner device.
- **Context at handoff:** ~62%.

### 2. Completed work

- **Decision: ship MVP on the generated `complete-roofing-lexicon.zeabur.app` domain.** Custom subdomain bind deferred. Placeholder `lexicon.justennewton.media` left in `<head>` canonical/og:url (still commented out) and Plausible `data-domain`; only `og:image` + `twitter:image` were repointed at the live Zeabur host so social cards resolve today.
- **og-cover.png generated** via Gemini 3.1 Flash Image Preview (inference.sh) — 16:9 2K source cropped/resized to exact 1200×630, ~992 KB. Composition matches in-app brand (Playfair masthead, redline divider, Cormorant italic subtitle, JN seal, faint low-slope roof cross-section).
- **Supabase REST gateway validated end-to-end.** Anon key authenticates against `https://rkasupabase.zeabur.app/rest/v1/`; all three tables (`roofing_training_staff`, `_progress`, `_certifications`) already exist; `supabase-setup.sql` was previously run on 2026-05-19; anon INSERT works (HTTP 201).
- **Supabase sync code fixed twice.**
  1. `sbTest()` was probing with SELECT, blocked by RLS (only INSERT is granted to anon by design) → rewrote to do the same write the production sync does, tagged `staff_name = '__sync_test__'`.
  2. After that, the owner hit `Insert blocked: Invalid authentication credentials` — root cause: malformed pasted key (truncation / internal whitespace / wrong key). Added `sbCleanKey()` (strips ALL whitespace), `sbParseAnonKey()` (client-side JWT validation, refuses `service_role` before it can reach the wire), friendlier 401/RLS error messages, and a non-secret Saved-key preview (`first6…last6  (N chars)`) under the key field so operators can paste-compare against Studio. Verified via Playwright against live Supabase: 8/8 input variations produce the right outcome.

#### Commits this session (newest first)

```
37456ca fix(sync): pre-validate anon key + strip internal whitespace + show preview
6ccefc4 fix(sync): make Test button exercise the actual write path
646f344 feat(branding): add og-cover.png (1200x630) for social previews
bacf07a chore: deployment state + RLS policies + handoffs through 2026-05-22
```

Files changed across the four commits: 8 (`.gitignore`, `CLAUDE.md`, `CLAUDE_CODE_HANDOFF.md`, `HANDOFF-2026-05-21.md`, `HANDOFF-2026-05-22.md`, `supabase-setup.sql`, `index.html`, `og-cover.png`).

### 3. Remaining tasks

1. **Owner: retry Test in Manager → Sync Settings** with the fixed JS. If 401 returns, the new Saved-key preview reveals exactly what's stored — paste-compare against Studio → API Settings.
2. **Owner: run one real cert end-to-end** with sync enabled → confirm a row lands in `roofing_training_certifications`.
3. **Cleanup test rows in Supabase** when convenient: `delete from roofing_training_progress where staff_name in ('__cli_preflight__','__cli_recheck__','__sync_test__');`
4. **Rotate Supabase Postgres `PASSWORD`** in Zeabur dashboard — the literal value was pasted into chat earlier today. Regenerate JWTs alongside if the self-hosted template re-derives them from `JWT_SECRET`.
5. ~~**Custom subdomain — apex domain now owned.**~~ **DONE 2026-05-22.** App is live at `https://lexicon.justennewton.media` (HTTP 200, Caddy-issued cert). `og-cover.png` serves from the new host (992 KB, HTTP 200). Three `index.html` edits applied (canonical + og:url uncommented, og:image + twitter:image flipped to custom domain). Plausible `data-domain` was already correct. Historical record of what was done:
   - In Namecheap → Domain List → `justennewton.media` → Manage → **Advanced DNS**, add a CNAME record: Host = `lexicon`, Target = `complete-roofing-lexicon.zeabur.app`, TTL = Automatic.
   - In Zeabur dashboard (project `6a0bec24c08ad7fb8a7dbb3a` → service `6a0bec48c08ad7fb8a7dbb40`) → Domains → Add Custom Domain `lexicon.justennewton.media`. Zeabur issues the cert automatically.
   - After DNS propagates (usually <30 min on Namecheap): three edits in `index.html`, one commit:
     1. Uncomment `<link rel="canonical">` (lines 9–10)
     2. Uncomment `<meta property="og:url">` (lines 17–18)
     3. Flip `og:image` (line 20) and `twitter:image` (line 24) from `https://complete-roofing-lexicon.zeabur.app/og-cover.png` → `https://lexicon.justennewton.media/og-cover.png`
     Plausible `data-domain` (line 28) is **already** `lexicon.justennewton.media` — no change needed there; it starts reporting correctly the moment the bind is live.
   - Note: the sibling Field Campaign also wants `play.justennewton.media` on the same apex — same Namecheap → Advanced DNS, A record Host = `play`, Value = `91.99.230.200` (already noted in §3 of the Field Campaign section above).
6. **`og-cover-2026-05-22.png`** is single-source; if you want stylistic variants generated, inference.sh credits are now back online (`infsh me` confirms account active).

### 4. Known blockers

- None active.
- Earlier in session: inference.sh out of credits (carried over from yesterday's HANDOFF) — user topped up, resolved.
- Local http.server tests on port 8765 ran clean (Playwright 0 unforced console errors); server shut down before commit.

### 5. Exact resume command

```powershell
cd "C:\Users\roofk\Dev\--FINAL ORGANIZED\The Commercial Roofing Lexicon"
```

Then either:

- **Open the deployed app** at https://complete-roofing-lexicon.zeabur.app/ → Manager (passcode `ROOF123`) → Sync Settings → click **Test Connection**. Expected: green "Connection OK..." message. If red, the Saved-key preview below the field is the diagnostic.
- **Or pivot to the Field Campaign repo** (see top of this file): `cd "C:\Users\roofk\Dev\--FINAL ORGANIZED\roofing-lexicon-field-campaign"` and run the prepped cyanotype validation batch.
